It doesnt use the standard core, but the EarlePhilHower pico core. So use that one.
It also needs the USB STACK option to be set as "ADAFRUIT TINY USB" for it to compile correctly.
Feel free to share and send something interesting that you may do with this code
